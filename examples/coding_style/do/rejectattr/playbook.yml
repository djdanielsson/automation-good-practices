---
- name: Rejectattr filter example
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Do something
      ansible.builtin.set_fact:
        filtered_list: >-
          {{ list_of_dict | rejectattr('type', 'search', '^bad_type$') | list }}
